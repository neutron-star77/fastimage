# Domain Docs

Single-context repo: CONTEXT.md at root + docs/adr/.

## Before exploring, read these
- CONTEXT.md at the repo root
- docs/adr/ — read ADRs relevant to the area you're working in

If files don't exist yet, proceed silently. domain-modeling creates them lazily.
